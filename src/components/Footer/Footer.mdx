import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
 
import * as FooterStories from './Footer.stories';
 
<Meta of={FooterStories} />
 
# Footer
 
Provides service information at the bottom of every page.

## Guidelines

- The Footer must be used on all CoConstruct websites.
- It must be the same on every page of the website.

## Props

<Canvas of={FooterStories.Default} />
<Controls of={FooterStories.Default} />
